# 092 — .env 형식 참고(시크릿 없음). 실제 값 생성:
#   CREDS_KEY=$(openssl rand -hex 32) / CREDS_IV=$(openssl rand -hex 16) / JWT_*=$(openssl rand -hex 32)
APP_TITLE=SEP GraphRAG
HOST=0.0.0.0
PORT=3080
MONGO_URI=mongodb://mongodb:27017/LibreChat
ALLOW_REGISTRATION=true
ALLOW_EMAIL_LOGIN=true
CREDS_KEY=
CREDS_IV=
JWT_SECRET=
JWT_REFRESH_SECRET=
