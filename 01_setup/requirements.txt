# 의존성. 단계별로 추가한다. (venv: uv 사용 → `uv pip install -r 01_setup/requirements.txt`)

# Phase 1 — 스크래핑/파싱 (021, 022)
httpx>=0.28
beautifulsoup4>=4.13
lxml>=5

# Phase 1 — 그래프 (023)
networkx>=3.4
pyvis>=0.3.2
numpy>=1.26      # networkx pagerank/louvain
scipy>=1.11      # networkx pagerank

# Phase 3 — 임베딩/인덱싱 (041, 042)
torch>=2.2                  # MPS(Apple Silicon)
sentence-transformers>=3
qdrant-client>=1.9
transformers>=4.40          # Qwen3 토크나이저/리랭커

# Phase 7 — 챗봇 UI (081)
streamlit>=1.30

# Phase 8 — 챗봇 v2 core API/셰임 (091, 092)
fastapi>=0.115
uvicorn>=0.30

# dev
ruff>=0.6
pytest>=8
