# 복사: cp .env.example .env  (그리고 docker compose / 스크립트가 자동 사용)
# Neo4j 비밀번호 — docker-compose(NEO4J_AUTH)와 to_neo4j/viz_neo4j 스크립트가 사용.
# 미설정 시 로컬 dev 기본값 sepgraph123 사용. 공개/원격이면 반드시 변경.
NEO4J_PASSWORD=sepgraph123
